from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='To train a custom neural networks model that will accurately predict a type of beer based on some rating criterias such as appearance, aroma, palate or taste. We will also need to build a web app and deploy it online in order to serve our model for real time predictions. ',
    author='Chanthru_Vimalasri',
    license='',
)
